"""Use: Marks `app` as the main backend Python package.
Where to use: Used automatically when Python imports modules from the `app` package.
Role: Package layer. It lets the backend treat the app folder as one importable package.
"""
